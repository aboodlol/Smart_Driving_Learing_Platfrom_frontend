export const environment = {
  production: false,
  // Leave empty for local development so that the proxy routing to localhost:3000 works and bypasses CORS.
  // Alternatively, put 'http://localhost:3000' here if your backend has allowed this origin in CORS policy.
  backendUrl: '' 
};
